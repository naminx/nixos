#!/usr/bin/env bash

if [ -z "$1" ]; then
    echo usage: ${0##*/} OUTPUT_BASE_NAME
else
    { cat - \
        | tee /dev/stderr 2>&1 1>&3 3>&- \
        | magick - "$1.jpg"; } 3>&1 1>&2 \
        | magick - "$1.webp"
    keepsmallest "$1.jpg" "$1.webp"
fi
